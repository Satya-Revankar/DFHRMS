<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- White Button background color -->
    <stroke
        android:width="1dp"
        android:color="@color/greenTextcolor" /> <!-- Border color with desired width -->
    <corners android:radius="30dp" /> <!-- Adjust the radius to your desired roundness -->
</shape>
